# 🚀 NovelAIne 향후 개발 계획안 (Roadmap & Next Steps)

현재 NovelAIne 프로젝트는 **핵심 스토리텔링 LLM 최적화, 이미지 생성 파이프라인 연동, 다중 에이전트 오케스트레이션, UI/UX 오버플로우 안정화** 등 핵심적인 기둥들을 성공적으로 세웠습니다.

이제 앱의 완성도를 상용 수준으로 끌어올리고, 유저의 리텐션(재방문율) 관여도와 시각/청각적 만족감을 높이기 위해 다음과 같은 추가 기능 개발을 제안합니다.

---

## 🎨 Phase 11: 몰입감 극대화 (감각적 확장)

### 1. BGM 및 사운드 이펙트(SFX) 자동 생성 연동 🎵
- **현재 상태**: `scenes` 모델 스키마에 `has_generated_bgm` 및 BGM 생성 트리거 로직 자리는 만들어져 있으나 실제 음원 생성 API는 미연동 상태입니다.
- **개발 계획**: Suno AI, 혹은 HuggingFace의 `MusicGen` API를 연동하여 특정 장면(감정/긴장도 점수가 높은 씬, 전투 씬, 슬픈 씬 등)에 어울리는 배경음악을 10~15초짜리로 실시간 합성해 재생장치(AudioPlayer)로 출력.

### 2. 이미지 생성(Text-to-Image) 고도화 및 프롬프트 주입 고도화 🖼
- **현재 상태**: 허깅페이스(HuggingFace) `Animagine XL` 모델로 이미지를 만들게 연결했으나, 캐릭터의 고정된 외모값을 매번 완벽히 반영하기엔 추가 세팅이 필요합니다.
- **개발 계획**: 
  - 캐릭터 생성 단계(CharacterStep)에서 유저가 지정한 `외모 특징(Appearance)`을 별도 DB 컬럼으로 분리.
  - 이미지 생성 프롬프트 조합 시 `[캐릭터 외모 프롬프트] + [현재 장면 프롬프트]` 구조를 강제하여, 아무렇게 생성해도 캐릭터 얼굴과 옷차림의 일관성(Consistency)이 유지되도록 LoRA/프롬프트 앙상블 적용.

---

## 👥 Phase 12: 소셜 및 커뮤니티 기능 (리텐션 강화)

### 1. 스토리 탐색 및 공유 (Public Library) 📚
- **현재 상태**: 생성된 소설은 본인만 열람 가능한 구조입니다. (현재 DB는 내 서재 위주)
- **개발 계획**: 유저가 완결된 스토리 혹은 매력적인 첫 장면을 **'공개(Public)'** 상태로 전환할 수 있는 버튼 추가.
- `ExploreScreen` 탭을 신설하여, 다른 유저들이 만들어낸 재밌는 인터랙티브 소설 리스트를 볼 수 있도록 구성 (좋아요/조회수/장르 필터링 기능).

### 2. 가지치기 (Fork & Branching) 시스템 🌿
- **설명**: 다른 유저가 만들어둔 공개 소설을 읽다가 "내가 여기서 다른 선택지를 골랐더라면?" 이라는 상상을 실현할 수 있게, 해당 씬(Scene)에서부터 스토리를 복제(Fork)해 자신만의 결말을 쓰는 분기점 시스템 개발.

---

## ⚙️ Phase 13: 개인화 시스템 (My Workspace)

### 1. 나만의 AI 커스텀 (Advanced Settings) 🎛
- **개발 계획**: 설정(Settings) 메뉴에서 유저가 LLM의 **Temperature (창의성/광기 조절)**, 문체 (하드보일드, 동화풍, 극사실주의) 등을 직접 튜닝할 수 있는 커스텀 다이얼 UI 제공.

### 2. 크레딧(Credit) 시스템 도입 및 결제 UI 💰
- **현재 상태**: API 사용 비용(Groq, HuggingFace 등)을 무제한 감당해야 합니다.
- **개발 계획**: 매일 유저 접속 시 '5 Story Tokens' 부여. 텍스트 진행 시 0.5 코인, 이미지 생성 시 1 코인 차감 로직 추가. (DB `profiles` 또는 `wallets` 테이블 신설)

---

## 🛠 Phase 14: 프론트/백엔드 아키텍처 및 DevOps 안전망

### 1. 프론트엔드 캐싱 (Hive / SharedPreferences) ⚡️
- **현재 상태**: 매번 새로고침 시 네트워크 요청으로 데이터를 불러옴.
- **개발 계획**: 읽고 있는 소설 데이터와 이미지를 로컬 스토리지(Hive 등)에 캐싱하여, 네트워크 레이턴시 없이 초고속 열람 및 오프라인 읽기(Read-only) 모드 제공.

### 2. CI/CD 자동화 구축 (GitHub Actions) 🤖
- **개발 계획**: 앞서 설정한 `자동 Git 버전 관리` 시스템과 연계하여, 코드가 Origin에 Push되면 자동으로 백엔드 테스트(Pytest), 프론트엔드 빌드(Flutter Web/APK build)가 돌아가고 이상이 없으면 클라우드(Vercel, Render/Railway, Supabase) 등으로 무중단 배포되는 파이프라인 구성.

---

## 🎈 제안 요약

우선적으로 체감 효과가 가장 큰 **Phase 11. BGM 연동 및 이미지 일관성 향상**을 진행하거나, 사용자 참여율을 끌어올리기 위한 **Phase 12. 소셜 도서관 탭 신설** 중 하나를 먼저 착수하는 것을 추천드립니다. 

마음에 드시는 방향을 하나 골라주시면, 즉시 오케스트레이션 봇을 깨워 기획/코딩 루프를 돌리겠습니다!
