---
title: "[1인 풀스택] LLM 기반 멀티 에이전트 자율 오케스트레이션 봇(Orchestration Bot) 구축기"
tags: ["LLM", "Multi-Agent", "Orchestration", "Automation", "AI", "DevOps"]
---

## 🤖 혼자서도 팀처럼: 멀티 에이전트 오케스트레이션(Orchestration)

최근 진행 중인 1인 개발 프로젝트인 **NovelAIne**의 규모가 점차 커지면서, 프론트엔드(Flutter)부터 백엔드(FastAPI), 그리고 UI/UX 기획에 이르기까지 신경 써야 할 도메인이 기하급수적으로 늘어났습니다.

매번 컨텍스트 스위칭을 하며 코드를 수정하는 비용을 줄이고자, **코딩 에이전트가 각 직군별 페르소나(Persona)를 자동으로 바꿔가며 스스로 기획부터 배포까지 수행하는 자율 오케스트레이션(Autonomous Orchestration)** 구조를 제 로컬 환경에 도입했습니다.

과연 AI 에이전트들을 어떻게 팀처럼 묶어서 글로벌 워크플로우로 굴렸는지, 그 설정 방법론을 블로그에 공유합니다!

---

## 1. 📂 에이전트 스킬(SKILL) 구조의 전역(Global) 분리

과거에는 특정 파이썬 프로젝트 폴더 내부에 프롬프트를 텍스트 파일로 때려 넣곤 했습니다. 하지만 이것은 프로젝트 종속적이라는 한계가 있습니다.

제가 원하는 것은 터미널 어디에서나 (마치 회사에 출근하는 직원들처럼) 에이전트들을 호출하는 것이었습니다. 그래서 홈 디렉토리 직속으로 글로벌 에이전트 폴더(`.agents`)를 개설하고, 각각의 **직군별 SKILL(역할망)**을 분리했습니다.

```bash
~/.agents/
├── skills/
│   ├── product_manager/     # [기획자] 요구사항 분석 및 Task 분배
│   │   └── SKILL.md
│   ├── ui_ux_designer/      # [디자이너] 앱 레이아웃 및 여백/오버플로우 계획
│   │   └── SKILL.md
│   ├── frontend_dev/        # [프론트] Flutter, Dart 위젯 코드 생성 구역
│   │   └── SKILL.md
│   └── backend_dev/         # [백엔드] FastAPI, DB, Python 로직 구역
│       └── SKILL.md
└── workflows/
    └── orchestrate.md       # 핵심! 이들을 지휘하는 워크플로우 명세서
```

---

## 2. 📝 직군별 페르소나(SKILL.md) 부여하기

각 직군 폴더에 있는 `SKILL.md`는 에이전트가 돌변해야 할 인격적 가이드라인입니다. 예를 들어 프론트엔드 담당자의 뇌세포를 다음과 같이 세팅했습니다.

**`~/.agents/skills/frontend_dev/SKILL.md` (예시)**
```markdown
---
name: Frontend Developer (Flutter)
description: Flutter/Dart UI 설계 및 상태 관리 담당
---

# 역할
당신은 최고의 Flutter 개발자입니다. Riverpod을 기반으로 한 반응형 상태 관리를 지향합니다.

# 제약 사항
1. 하드코딩된 사이즈(예: SizedBox(height: 500))를 피해 오버플로우를 막으세요.
2. 항상 Expanded, Flexible, SafeArea를 적극 사용하여 기기 대응을 하세요.
```
이렇게 각 직군의 페르소나를 독립적으로 구성함으로써, 에이전트가 코드를 짤 때 백엔드/프론트엔드 관점이 모호하게 섞이는 환각(Hallucination) 현상을 차단할 수 있습니다.

---

## 3. 🎼 `/orchestrate` 자율 워크플로우 설계

이제 흩어져 있는 이 직원들을 모아서 릴레이 달리기를 시킬 **지휘자(Orchestrator)** 규칙 파일이 필요합니다. `/workflows/orchestrate.md` 파일을 생성하고 아래처럼 단계별 절차를 선언합니다.

**`~/.agents/workflows/orchestrate.md`**
```markdown
---
description: 다중 에이전트를 가동해 기획부터 풀스택 테스트까지 자동 완료하는 자율 루프
---

이 워크플로우는 사용자가 `/orchestrate [요구사항]`을 입력할 때 트리거됩니다.
에이전트는 사용자의 즉각적인 개입 없이 다음 단계를 순차적으로, 자율적으로 이행해야 합니다!

1. **[Product Manager 호환]**: 사용자 요구사항을 분석하고 `task.md` 에 체크리스트 작성.
2. **[UI/UX Designer 호환]**: 디자인적 컴포넌트 추가가 필요하다면 `implementation_plan.md` 에 설계 작성.
3. **[Frontend & Backend Dev 호환]**: 계획서를 토대로 실제 코드를 교체/작성 및 터미널 테스트 실행.
4. **[Validation]**: 앱에서 예외나 에러(예: 422, 500)가 없는지 스스로 터미널 디버깅.
5. 루프의 마지막에 사용자에게 `[완료 보고서]` 통지하기.
```

---

## 4. 🚀 어떻게 작동하는가? (실전 사례)

터미널에서 에이전트에게 챗으로 툭 던집니다.
> *"사용자 프로필 탭 화면에서 폰이 작으면 아래쪽 UI가 잘리는 이슈 해결하고 /orchestrate 워크플로우 돌려줘. 중간에 나한테 묻지 말고 알아서 기획하고 코드 짜고 디버깅 해서 끝내."*

이렇게 명령을 내리면, 에이전트 봇은 혼자서 다음과 같이 놉니다.

1. **(PM 변신)**: "앗, 목표 수령! `task.md`에 'Profile Tab 오버플로우 수정안' 작성 완료."
2. **(UI/UX 변신)**: "고정된 `Column` 위젯이 문제구만, `NestedScrollView` 로 교환하겠다는 안을 `implementation_plan.md`에 채택!"
3. **(Frontend 변신)**: "계획서대로 `profile_screen.dart`를 찾아가서 Flutter 코드 치환 후 `flutter run` 검토."
4. **(마무리)**: "수정 완료! 사용자님, 지시하신 에러 픽스 정상 구동 확인되었습니다."

### 💡 고찰 및 성과
혼자서 수만 줄의 코드를 작성하다 보면 "이건 프론트에서 데이터 가공을 하는 게 낫나, 백엔드에서 내려주는 게 낫나?" 하고 뇌 정지가 오는 구간이 있습니다. 

글로벌 워크플로우를 세팅하고 나니, **"나는 지시하는 팀장, 에이전트는 기획/개발/디버깅을 분업하는 실무자 부서"** 의 완벽한 1인 개발 애자일 뷰가 형성되었습니다. 이제 단순 타이핑과 에러 트래킹을 AI에게 위임한 덕분에, 저는 제품의 재미 요소와 퀄리티 고도화에만 온전히 집중할 수 있게 되었습니다.😎

---
이런 식으로 자신의 업무 도메인과 템플릿에 맞게 `.agents` 룰 셋을 짜보시면, 생산성이 무서울 정도로 폭발하실 거라고 확신합니다!🔥
