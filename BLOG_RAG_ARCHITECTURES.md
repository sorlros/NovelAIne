# AI 소설가의 완벽한 기억력: NovelAIne의 지능형 RAG 아키텍처 설계기

AI와 함께 긴 소설을 써본 사람이라면 누구나 겪는 문제가 있습니다. 바로 AI가 **"방금 전까지 같이 싸우던 동료의 이름을 잊어버리거나"**, **"이미 파괴된 성문에 도착했다고 서술하는"** 설정 오류(Hallucination)입니다.

NovelAIne은 이 문제를 해결하기 위해 **Supabase pgvector**와 **지능형 트리거 시스템**을 결합한 고도화된 RAG 시스템을 구축했습니다. 본 포스팅에서는 NovelAIne에 적용된 RAG 기술의 핵심과 그 이면의 결정들을 공유합니다.

---

## 1. 왜 RAG(Retrieval-Augmented Generation)인가?

LLM(거대언어모델)은 한 번에 처리할 수 있는 '컨텍스트 윈도우'에 한계가 있습니다. 소설이 길어질수록 과거의 복잡한 설정들을 모두 프롬프트에 넣을 수 없게 되고, 이는 곧 비용 상승과 성능 저하로 이어집니다.

RAG는 모든 정보를 AI에게 주지 않고, **"지금 이 순간 꼭 필요한 정보만 데이터베이스에서 찾아 전달"**하는 기술입니다. NovelAIne은 이를 통해 무한한 길이의 서사에서도 일관성을 유지합니다.

## 2. 기술적 핵심: Supabase + pgvector

NovelAIne은 벡터 데이터베이스로 **Supabase의 pgvector**를 선택했습니다.

*   **선택 이유**: 관계형 데이터(사용자 정보, 스토리 목록)와 벡터 데이터(세계관 설정, 캐릭터 임베딩)를 하나의 DB에서 관리할 수 있어 아키텍처가 단순해집니다.
*   **효과**: 캐릭터의 외형 묘사나 과거 사건을 384차원의 벡터로 변환하여 저장하고, 유저의 현재 입력과 가장 유사한 맥락을 밀리초(ms) 단위로 찾아냅니다.

## 3. 혁신: Smart RAG Triggers

모든 대화마다 RAG를 가동하는 것은 비효율적입니다. "응", "알았어" 같은 짧은 대답에도 DB를 검색하면 응답 속도만 느려지기 때문입니다. 우리는 **4가지 지능형 트리거**를 설계했습니다.

1.  **명시적 조회 (`?`)**: 유저가 질문을 던지면 설정을 확인해야 할 신호로 판단합니다.
2.  **복잡도 기반 (25자 이상)**: 문장이 길어지면 구체적인 서사 맥락이 필요하다고 인지합니다.
3.  **서사적 키워드 감지**: '기억', '정체', '예전', '비밀' 등 과거 데이터 참조가 필요한 단어를 실시간 스캔합니다.
4.  **시공간 전환 감지**: '다음 날', '이동', '도착' 등 장소나 시간이 바뀔 때 새로운 환경 설정을 불러옵니다.

## 4. 성능 최적화: English-Centric & Keyword Logic

토큰 효율과 추론 성능을 위해 프롬프트 전달 방식을 혁신했습니다.

*   **영문 최적화**: 내부적으로 RAG 데이터를 영문 키워드로 변환하여 LLM에 전달합니다. 이는 한국어 대비 **2~3배의 토큰 절감 효과**와 더불어, 영문 데이터 학습량이 많은 LLM의 **추론 정확도를 극대화**합니다.
*   **구조적 키워드 분할**: 자연어 문장 대신 `[Location: Castle, Mood: Dark]`와 같은 구조화된 키워드 블록을 사용하여 AI가 설정을 오해할 여지를 차단했습니다.

## 5. 서사 모드별 차별화 (Hero vs Ensemble)

NovelAIne은 서사의 성격에 따라 RAG의 활용 방식이 달라집니다.

*   **1인 주인공 모드**: RAG가 **주인공의 과거사와 능력치**를 우선적으로 찾아내어 유저가 선택한 주인공 페르소나를 강화합니다.
*   **군상극 모드**: RAG가 **인물 간의 관계망과 세계관의 전설**을 위주로 검색하여, 한 명의 영웅이 아닌 세계 전체의 역동적인 변화를 서술하도록 돕습니다.

---

## 결론: 창작자를 위한 '외장 하드'

NovelAIne의 RAG 시스템은 단순히 데이터를 찾아주는 검색 엔진이 아닙니다. 작가의 머릿속에만 있던 방대한 설정을 AI가 완벽하게 공유하게 해주는 **'창작용 외장 브레인'**입니다.

우리는 이 기술을 통해 사용자가 설정 오류에 대한 걱정 없이 오직 **'다음 이야기에 대한 상상'**에만 집중할 수 있는 환경을 만들고 있습니다.

---
**Tech Stack Summary:**
- **Vector DB**: Supabase (PostgreSQL + pgvector)
- **Embedding**: HuggingFace (All-MiniLM-L6-v2)
- **Framework**: FastAPI (Python) & Flutter (WASM)
